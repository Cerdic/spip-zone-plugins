<plugin>
    <nom>À propos des Plugins actifs</nom>
    <auteur>[Jean-Philippe Guihard->http://www.jp.guihard.net/]</auteur>
    <version>0.ß2</version>
    <etat>dev</etat>
    <description>
    <multi>
	[fr] Liste les plugins actifs et affiche une description sommaire de ceux-ci.
    
    Il sert à afficher sur une page de type à propos du site un récapitulatif des plugins actifs, y compris l'icone de ceux-ci.
    
	Si le plugin est actif, on peut faire apparaitre la liste dans un squelette en utilisant un <code><INCLURE{fond=apropos}></code>.<br />
	
	Pour afficher la liste dans un article, il faut utiliser le modele <code><apropos|x></code>. En d'autres termes, copiez-collez <code><apropos|x></code> dans le texte de votre article.
	[en]Lists the active plugins and displays a brief description of them.
	It is used to display a page of type about the site a summary of active plugins, including the icon of these.
	
	If the plugin is active, you can bring up the list in a template using a <code><INCLURE{fond=apropos}></code>. <br />

	To view the list in an article, you must use the model <code><apropos|x></code>. In other words, copy and paste <code><apropos_plugin|x></code> in the text of your article.
    </multi>
	</description>
	<icon>apropos.png</icon>
    <prefix>apropos</prefix>
	<pipeline>
	  <nom>insert_head</nom>
		<action>insert_head</action>
		<inclure>apropos_pipeline.php</inclure>
	</pipeline>
	<necessite id="SPIP" version="[2.1.1;]" />
	<categorie>divers</categorie> 
</plugin>
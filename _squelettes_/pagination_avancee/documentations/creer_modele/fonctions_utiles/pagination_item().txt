pagination_item()

return ce qu'il y entre deux separateurs ou aux extremitŽs de la pagination (je sais pas trop comment expliquer) .

prend en parametre : ->$num : le numero qui va apparaitre dans l'url (debut_machin)

->$txt : il remplacera le @item@ du pattern(voir plus bas)

->$pattern : ce qui sera retourner. On peut y placer Ç@url@È qui sera remplacer par l'url de la page former ˆ partir de des autres paramtres. On peut Žgalement y placer @item@ qui sera remplacer par $txt

->$lien_base : le la page sur laquelele il faut renvoyer,gŽnŽralement self()

->$debut : le type de debut qu'il faut mettre.

->$ancre : la valeur de #ANCRE_Pagination. correspondra ˆ l'endroit.

par exemple

<code>pagination_item("xx","yy","<strong><a href='@url@'>@item@</a></strong>",'spip.php', "debut_zzz","balise");</code>

retournera

<code><strong><a href='spip.php?debut_zzz=xx#balise'>yy</a></strong></code>
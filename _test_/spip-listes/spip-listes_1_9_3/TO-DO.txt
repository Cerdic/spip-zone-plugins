
// $LastChangedRevision$
// $LastChangedBy$
// $LastChangedDate$


TO-DO:
- autoriser la création d'un courrier à un modérateur (il ne pourra l'envoyer qu'a une adresse de test ou une liste d'abonnés qu'il maintient)
- ne pas accepter l'import dans une liste qui n'a pas de grand patron (sans grand patron, le courrier de la liste est vide...)
- permettre dans l'import l'assignation d'un abonné à un groupe (redac ou invité) et donc revoir l'export
- optimiser le cron (j'ai comme l'impression que certains s'attendent à voir le courrier partir illico). Il y a peut-etre une astuce à trouver ici (calculer la charge du serveur, le nombre de plugin utilisant le cron, etc.) Des idées ?
- terminer la compat svn (fouiller!) CP20080609
- corriger les CSS pour espace public des formulaires (le cache, toujours le cache)
- revoir la doc docs/__installation_*.txt
- permettre l'import direct d'une image dans le courrier (sans lien). Un poil compliqué, mais faisable.
- corriger documentation contextuelle (to-do perpetuel ;-))
- maintenance: mettre en place l'auth. par FTP.
- module de stats (calendrier des envoyés/à envoyer pour vérifier engorgement)
- icone 'casier à courrier' à revoir. Idem 'listes de diffusion'


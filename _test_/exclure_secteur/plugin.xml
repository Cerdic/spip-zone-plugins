<plugin>
    <nom>Secteurs Exclus</nom>
    <auteur>
    Auteur originel : Ma&iuml;eul Rouquette 
    Licence : GPL3
    </auteur>
    <version>
    1.0
    </version>
    <etat>
    test
    </etat>
    <necessite id='CFG' />

    <description>
Exclut automatiquement les secteurs s&eacute;l&eacute;ctionn&eacute;s des boucles ARTICLES / SITES / RUBRIQUES / BREVES

    Attention, pour le moment, ne le fait pas sur les jointures (par exemple, n'exclura pas les forums qui appartiennent à un article du secteur).
    
    On peut faire une exception avec le crit&eacute;re <code>{tout_secteur}</code>. 
    Si on pr&eacute;cise le secteur explicitement, par <code>{id_secteur=x}</code>, <code>{id_secteur==x}</code> ou <code>{id_secteur IN X}</code>, le r&eacute;glage ne sera pas pris en compte.
    
    Possibilit&eacute; aussi de configurer le plugin pour que <code>{tout}</code> soit &eacute;quivalent à <code>{tout_voir}</code>.


	
		 
	</description>
    <prefix>
    exclure_sect
    </prefix>
        <pipeline>
        <nom>pre_boucle</nom>
        <fonction>pre_boucle</fonction>
        <fichier>exclure_sect_pipelines.php</fichier>
    </pipeline>

 
	<fonctions>exclurepip.php</fonctions>

</plugin>
<plugin>
	<nom>Traductions synchrones</nom>
	<auteur>Matthieu Marcillaud</auteur>
	<lien></lien>
	
	<icon>images/tradsync-32.png</icon>
	<licence>GNU/GPL</licence>
	
	<version>1.0.1</version>

	<etat>test</etat>
	<description>
	  Aide &agrave; la synchronisation des traductions.

	  En plus de l'interface, ce plugin fournit quelques crit&egrave;res :
-* <code>{traductions en}</code> : tous les &eacute;l&eacute;ments
	anglais etant des traductions = <code>{!origine_traduction}{lang=en}</code>
-* <code>{!traductions en}</code> : tous les &eacute;l&eacute;ments
	d'origine non traduits en anglais
-* <code>{origine_modifiee}</code> : tous les &eacute;l&eacute;ments
	traduits dont la source a &eacute;t&eacute; modifi&eacute;e
	(date plus r&eacute;cente)
-* <code>{!origine_modifiee}</code> : tous les &eacute;l&eacute;ments traduits
	dont la source n'est pas plus r&eacute;cente
-* <code>{polyglotte titre}</code> : tous les elements ayant une expression "multi" dans le titre
-* <code>{polyglotte titre,en}</code> : tous les elements ayant une expression "multi" poss&eacute;dant l'anglais dans le titre
-* <code>{!polyglotte titre}</code> : tous les elements n'ayant pas d'expression "multi"
-* <code>{!polyglotte titre,en}</code> : tous les elements n'ayant pas d'expression "multi" dans la langue demand&eacute;e

	</description>
	<prefix>tradsync</prefix>
	
	<fonctions>tradsync_fonctions.php</fonctions>
	
	<pipeline>
		<nom>autoriser</nom>
		<inclure>tradsync_autorisations.php</inclure>
	</pipeline>
	
	<!-- necessite id="SPIP" version="[2.1-beta;]" / -->
	<necessite id="SPIP" version="[2.0.10;]" />
	<necessite id="saisies" version="[1.6.6;]" />
	<necessite id="spip_bonux" version="[1.8.6;]" />
	<necessite id="afficher_objets" version="[1.0.1;]" />
	<necessite id="tradrub" version="[1.0.1;]" />
		
	<bouton id='traductions' parent='naviguer'>
		<icone>images/tradsync-22.png</icone>
		<titre>tradsync:espace_traducteurs</titre>
	</bouton>
</plugin>

<plugin>
	<nom> <!-- Nom du plugin -->
	Historique Agenda 2.0
	</nom>

	<icon></icon>

	<auteur>	<!-- Auteur du plugin -->
	Florent Jugla 	[Eledo->http://www.eledo.com]
_ &#169; ao&ucirc;t 2008 - Licence GPL
	</auteur>

	<version>2.0</version>

	<etat>test</etat>

	<description>
	Plugin permettant de visualiser les diff&eacute;rences sur les agendas.
_ {{Ce plugin n&#233;cessite le plugin Agenda 0.18 ou plus}}
	</description>

	<!-- fichier charge a chaque hit -->
	<!--options>inc/ha_mes_options.php</options-->

	<!-- fichier charge a chaque recalcul -->
	<fonctions>inc/ha_mes_fonctions.php</fonctions>

	<!--install>base/ha_install.php</install-->

	<prefix>ha</prefix>

	<!-- Definitions des fonctions qui s'inserent dans les pipelines -->
	<pipeline>
		<nom>ajouter_onglets</nom>
		<inclure>inc/ha_pipelines.php</inclure>
	</pipeline>

	<pipeline>
		<nom>taches_generales_cron</nom>
		<inclure>inc/ha_pipelines.php</inclure>
	</pipeline>

	<!-- bouton Edition->historique -->
        <pipeline>
                <nom>ajouter_boutons</nom>
                <action>ajbouton</action>
                <inclure>inc/ha_pipelines.php</inclure>
        </pipeline>

        <pipeline>
                <nom>header_prive</nom>
                <action>headerprive</action>
                <inclure>inc/ha_pipelines.php</inclure>
        </pipeline>

        <!--pipeline>
                <nom>ajouter_onglets</nom>
                <action>ajonglet</action>
                <inclure>inc/ha_pipelines.php</inclure>
        </pipeline-->

	<necessite id="SPIP" version="[1.9.0;]" />
	<necessite id="Agenda" version="[0.18;]" />
	<chemin dir='' />
</plugin>
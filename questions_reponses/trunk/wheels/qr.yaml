--- # Syntaxe des raccourcis SPIP, version 0.0.1
    #
    # Si on nomme une regle, elle devient surchargeable

faq:
  match: "%<faq([^>]*(?:format=([\"'])([^\"']+)\2)[^>]*|)>\n(.*?)\n</faq>%s"
  is_callback: Y
  replace: tw_qr
  priority: -40

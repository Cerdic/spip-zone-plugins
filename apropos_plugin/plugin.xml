<plugin>
    <nom>À propos des Plugins actifs</nom>
    <auteur>[Jean-Philippe Guihard->http://www.jp.guihard.net/]</auteur>
    <version>0.ß1</version>
    <etat>dev</etat>
    <description>Liste les plugins actifs et affiche une description sommaire de ceux-ci.
    
    Il sert à afficher sur une page de type à propos du site un récapitulatif des plugins actifs, y compris l'icone de ceux-ci.
    
	Si le plugin est actif, on peut faire apparaitre la liste dans un squelette en utilisant un <code><INCLURE{fond=apropos_plugin}></code>
	Pour afficher la liste dans un article, il faut utiliser le modele <code>à faire</code>
	</description>
	<icon>apropos_plugin.png</icon>
    <prefix>apropos_plugin</prefix>
      <pipeline>
	  <nom>insert_head</nom>
		<action>insert_head</action>
	  <inclure>apropos_plugin_pipeline.php</inclure>
  </pipeline>
</plugin>
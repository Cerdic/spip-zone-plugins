Notice rapide BoOz@rezo.net

x) Assurez vous d'avoir prealablement intallé les plugins
		- cfg
		- inscription2
		- acces restreint
		- balise session


x) Customiser la page commander.html

x) ajouter dans inc-entete en dessous de #FORMULAIRE_RECHERCHE

<INCLURE{fond=fonds/inc-entree_profil}>

x) Ajouter dans une page abonnement_complet.html

	<?php if($auteur_session) { ?>

			<BOUCLE_auteur(AUTEURS){id_auteur=#SESSION{id_auteur}}{tout}>
			<BOUCLE_auteur_elargi(AUTEURS_ELARGIS auteurs){id_auteur}{tout}{validite > #ENV{date}}>
			<BOUCLE_abo(spip_auteurs_elargis_abonnements){id_auteur_elargi = #ID}>
			<BOUCLE_abo2(spip_abonnements){id_abonnement}>
			Vous êtes déja abonné :
			<p>
			#LIBELLE - &eacute;ch&eacute;ance le [(#_abo:VALIDITE|affdate)]
			</p>
			</BOUCLE_abo2>
			</BOUCLE_abo>
			<div style="padding:5px;background-color:#F4F1D0">
							<form action="./?page=commander" method="post">
							[(#INCLURE{fond=formulaires/abonnement_liste}{abonnement=1})]	
							<input type="hidden" value="reabo" name="reabo">
							<input type="hidden" value="#SESSION{id_auteur}" name="id_auteur">
							<p style="text-align:right"><input type="submit" value="valider"></p>
							</form>
			</div>
			<//B_abo>
			</BOUCLE_auteur_elargi>
			</BOUCLE_auteur>
			

			<?php } else { ?>

			#FORMULAIRE_INSCRIPTION2

			<?php } ?>



x) Ajouter dans une page profil_complet.html

<INCLURE{fond=inc/profil_adherent}>

<INCLURE{fond=fonds/mes_abonnements}{id_auteur=#SESSION{id_auteur}}>





x) Mettre pour la page article d'une rubrique en acces restreint

<BOUCLE_article_principal(ARTICLES) {id_article}>

HTML ici

</BOUCLE_article_principal></BOUCLE_article_principal>
		[(#ID_ARTICLE|AccesRestreint_article_restreint|?{' ',''}) <INCLURE{fond=article_restreint}{id_article}> ] 
		[(#ID_ARTICLE|AccesRestreint_article_restreint|?{'',' '}) Cette page n'existe pas ] 
<//B_article_principal>


x) Relooker la page article_restreint.html


TODO :

Vérifier les index de spip_auteurs_elargis_abonnments et le auto increment
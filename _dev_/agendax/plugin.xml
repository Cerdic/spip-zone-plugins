<plugin>
	<nom> <!-- Nom du plugin -->
	Agendax
	</nom>
	<auteur>	<!-- Auteur du plugin -->
 	Sebastien D. ( crowf00t pour les intimes ;-p )
_ &#169; 2006 - Distribu&eacute; sous licence GPL
	</auteur>
	<version> <!-- Version du plugin -->
	0.1
	</version>
	<etat> <!-- Etat du plugin, au choix : dev (developpement), test, stable, experimental -->
	test
	</etat>
	<description>
	Un petit calendrier en ajax qui se base sur une seul boucle article (ou autre) avec un crit&egrave;re agenda.
_ 
_ Pour qu'il fonctionne, vous devez avoir la balise #INSERT_HEAD dans vos squelettes.
_ 
_ Vous pouvez modifier la boucle et les autres choses dans le fichier de données : donnees_agendax.html
_ 
_ Pour afficher l'agenda dans vos squelettes, il vous suffit de mettre un "div" avec un id='agendax'. Il y a une <a href="../?page=agendax">Demo ici</a>
_ 
_ Pour modifier l'apparence, vous pouvez modifier le fichier des css : agendax.css
	</description>
	<lien>http://www.spip-contrib.net</lien> <!-- URI de documentation -->
	<!-- prefixe des fonctions, filtres et fichers de votre plugin : 
		toutes vos fonctions doivent commencer par ce prefixe suivi de _ (ici : template_ma_fonction() par exemple)
		C'est par ailleurs conseille pour les noms de fichier
		Deux plugin ne peuvent pas avoir le meme prefixe, donc ne gardez pas celui la ! :)
		-->
	<prefix> 
	Agendax
	</prefix>
	<!-- Definitions des fonctions qui s'inserent dans les pipelines -->
	<!-- Le parametre action est facultatif : en son absence la fonction appelee sera le prefixe suivi du nom du pipeline
	ex : template_affichage_final()
	Le parametre inclure est egalement facultatif, si la fonction est deja chargee en memoire (car presente dans le fichier options par exemple)	-->
	<pipeline>
		<nom>insert_head</nom>
		<inclure>agendax.php</inclure>
	</pipeline>
</plugin>

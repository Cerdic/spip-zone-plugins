<plugin>
	<nom> <!-- Nom du plugin -->
	Boucles XML
	</nom>
	<auteur>	<!-- Auteur du plugin -->
	Cedric MORIN [notre-ville.net->http://notre-ville.net]
_ &#169; 2006 Licence GPL
	</auteur>
	<version>
	0.10
	</version>
	<etat>
	test
	</etat>
	<description>
	Parser du xml avec les boucles SPIP
	
	Le plugin introduit un type de boucle XML qui permet de parcourir l'arborescence d'un fichier au format
xml au moyen des boucles spip. Les crit&#232;res disponibles sont
-* <code>{xml=fichier.xml}</code> pour aller chercher fichier.xml dans le path
-* <code>{xpath}</code> pour acceder a un noeud par son chemin a partir de la racine (fini par un /}
-* <code>{racine}</code>
-* <code>{id_parent}</code>
-* <code>{statut=noeud} {statut=feuille}</code>
-* <code>{noeud=MonElement}</code> pour acceder directement a un noeud independamment de son arborescence
-* <code>{attributs==monAttr=['"]truc['"]}</code> pour acceder directement a un noeud dont l attribut monAttr vaut truc
-* NIY <code>{id_enfant}</code>

Les balises utilses sont #XPATH, #NOEUD, #ATTRIBUTS (sur laquelle on utilisera le filtre <code>|extraire{}</code>), et #TEXTE pour les feuilles (vide pour les noeuds)
	</description>
	<lien></lien> <!-- URI de documentation -->
	<options> <!-- fichier charge a chaque hit -->
	xml_options.php
	</options>
	<fonctions> <!-- fichier charge a chaque recalul de page -->
	xml_fonctions.php
	</fonctions>
	<prefix> 
	xml
	</prefix>
	<!-- Le parametre action est facultatif : en son absence la methode de la classe portant le nom du pipeline est appelee. Le parametre inclure est facultatif.	-->
</plugin>